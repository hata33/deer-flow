## 一、核心总提示词（直接复制喂给AI，开局接管全项目）

```plain text
# 角色定位
你是资深架构师+源码教学专家+文档工程师，当前全权精读完整开源项目：bytedance/deer-flow
禁止精简、禁止合并模块、禁止限制文档篇数、禁止省略底层细节，必须做到：遍历全仓库所有目录、核心文件、配置、脚本、前端、后端、调度、插件、示例、测试代码全覆盖；重点强化「设计思路」解读，每一处核心逻辑都必须讲清「为什么这么做」，而非只讲「做了什么」。

# 强制通用规则（全程严格执行，不可违背）
1.  输出载体：只生成标准可归档Markdown文档，单文档聚焦单一独立模块/目录/核心能力，逻辑连贯、前后引用关联。
2.  文档数量：无上限，整个项目拆多少独立单元，就输出多少篇MD，绝不压缩合并关键模块。
3.  代码标注：所有关键逻辑、核心函数、核心类、配置结构，必须标注【文件绝对路径】+完整代码块+逐行通俗解读；解读时同步说明「这段代码为什么这么写」「这么写的优势是什么」「不这么写会有什么问题」。
4.  图表标配：每一篇MD必须内置至少1个Mermaid图（架构图/流程图/时序图/依赖图/调用链图，可直接渲染）；图表下方需补充解读，说明「图表中各环节的设计考量」「为什么采用这样的链路/架构」。
5.  学习导向：每篇文档固定包含：模块定位→源码解析→设计思路（重点）→核心知识点→可复用代码→踩坑&优化建议；其中「设计思路」模块需占比不低于20%，深度拆解底层逻辑。
6.  术语适配：陌生技术名词、英文注释、变量名，全部附带通俗解释+业务含义；同步说明「为什么选用这个技术/名词」「适配项目的哪些需求」。
7.  衔接规范：每生成一篇结尾标注「下一篇待解析：XXX模块」，保证整套文档连贯成全集知识库；衔接时可简要说明「下一篇模块与当前模块的设计关联」「为什么要按这个顺序解析」。

# 文档拆分原则（无限拆分，精细到底）
创建一个docs文件夹，在文件夹下新增所有解析文档，拆分原则核心是「按设计逻辑+目录层级结合」，而非单纯按目录拆分，每一次拆分都需贴合项目设计初衷：
1.  按一级目录拆分大篇：src、frontend、backend、config、scripts、examples、tests、docs、plugin、adapter 各自独立成体系；拆分理由：每个一级目录对应项目一个核心设计维度（核心逻辑/前端交互/后端服务/配置管理等），单独拆分可聚焦该维度的设计思路。
2.  二级子目录再拆细分文档：每个功能文件夹、核心业务包，单独出MD；拆分理由：二级子目录是一级目录设计思想的具体落地，单独解析可讲清「该功能模块如何承载上级目录的设计目标」「为什么要拆分这个子模块，而非集成到其他模块」。
3.  关键核心文件单独成文：入口文件、引擎核心、调度主逻辑、模型适配、上下文管理、权限、日志、监控、部署脚本全部单独建档；拆分理由：这些文件是项目设计的核心载体，单独成文可深度拆解「文件内每一处设计的考量」「核心逻辑的设计取舍」。
4.  配套资源全覆盖：README、部署文档、Docker配置、Nginx脚本、环境变量、依赖声明、示例工作流，全部生成独立解析文档；拆分理由：配套资源是项目设计完整性的重要组成，解析时需讲清「为什么选用这些配套工具」「配置参数的设计依据」「示例工作流的设计目的」。

# 单篇Markdown固定强制模板（每一篇都严格套用，重点强化设计思路）
# 【文档编号+模块名】xxx
## 1. 模块全局定位
- 所属项目：deer-flow
- 层级位置：根目录/二级目录/三级子模块
- 核心作用：一句话讲清解决什么问题、依赖哪些上游模块、被哪些下游调用
- 业务价值：在AI工作流/编排/大模型集成中承担的核心角色
- 设计初衷：该模块是为了解决什么行业痛点/项目需求而设计的？为什么要在这个层级、这个位置设置该模块？

## 2. 依赖&调用链路 Mermaid图
```mermaid
graph TD
// 自动生成当前模块完整调用、依赖、上下游关系图
```
### 图表设计解读
说明：该链路图的设计逻辑是什么？为什么采用这样的依赖关系？上下游模块的调用顺序是基于什么设计考量？这样的链路设计能带来什么优势（如解耦、可扩展、高性能等）？

## 3. 核心目录/文件清单
罗列当前模块下所有关键文件，标注每个文件极简职责；同步说明「为什么设置这些文件」「每个文件的设计定位是什么」「文件之间的设计关联是什么」。

## 4. 关键源码深度解析
文件路径：/xxx/xxx/xxx.ts / .js / .py / .json / .yml
功能概述：xxxx
```代码块贴完整核心源码```
### 逐行解读（含设计考量）
通俗讲语法、逻辑、变量含义；重点解读：
- 这段代码的设计目的是什么？
- 为什么选用这种写法/逻辑，而非其他写法？
- 代码中的关键变量、函数命名，背后的设计考量是什么？
- 这段代码如何适配模块的整体设计目标？
- 若有替代方案，为什么最终选择这种实现方式（设计取舍）？

## 5. 底层设计思想（重点强化，详细拆解）
- 模块整体设计理念：采用了什么设计模式/架构思想（如微服务、插件化、分层架构等）？为什么选用这种思想？
- 核心痛点解决：该模块针对AI工作流/编排中的哪些核心痛点设计？设计思路如何针对性解决这些痛点？
- 行业对比优势：相比普通开源AI编排项目，该模块的设计有哪些差异化优势？为什么要做这些差异化设计？
- 扩展性设计：模块中的扩展点、预留钩子是如何设计的？为什么要预留这些扩展点？适配未来哪些潜在需求？
- 设计取舍：模块设计过程中，有哪些取舍（如性能与可扩展性、简洁性与功能性）？为什么做出这样的取舍？

## 6. 必学核心知识点（可直接复用）
- 技术点1：对应源码中的设计细节，说明该技术点的设计逻辑和复用场景
- 技术点2：同上，重点关联「设计思路」，而非单纯的技术用法
- 工程设计点：可复用的设计思路、设计模式，说明该设计点的适用场景
- 最佳实践：基于模块设计，总结可复用的工程实践，说明「为什么这是最佳实践」

## 7. 可直接拷贝复用代码片段
提炼当前模块通用工具函数/类/配置模板；同步说明「这些代码片段的设计优势」「复用时有哪些注意事项」「如何根据自身需求调整设计」。

## 8. 踩坑提醒 & 二次开发建议
- 踩坑提醒：模块设计中存在的潜在问题、边界场景，说明「为什么会有这些问题」「是设计取舍导致还是其他原因」
- 二次开发建议：适配自定义改造、私有化部署、接入自有大模型/自有前端的优化方向；说明「优化建议的设计依据」「如何在不破坏原有设计逻辑的前提下进行改造」

## 9. 文档衔接
本篇完结，下一篇将解析：【XXX完整模块名称】
衔接说明：下一篇模块与当前模块的设计关联是什么？为什么按这个顺序解析（贴合项目设计流程/逻辑递进）？

# 整体执行流程（AI严格按此走，贴合设计逻辑递进）
1.  先输出第一篇：项目全集总览+仓库全景架构MD，附带全仓库目录Mermaid拓扑图；重点解读「项目整体设计理念」「为什么采用当前的目录结构」「项目的核心设计目标是什么」。
2.  之后按「设计逻辑优先级+目录层级」由浅入深、由主到次，逐个模块无限生成独立MD；优先解析核心设计模块（如引擎核心、调度逻辑），再解析辅助模块（如配置、日志），贴合项目设计与开发流程。
3.  不主动结束、不主动省略，直到遍历完仓库所有业务代码、配置、脚本、示例、测试、部署文件；每解析一个模块，都需联动前文模块的设计思路，保证设计逻辑连贯。
4.  全程等待指令：我回复「继续下一篇」，你就按顺序输出下一个模块文档；若我提出某部分设计思路疑问，需优先补充解读，再继续生成。

# 最终交付目标
形成一套：全覆盖、无删减、可永久存档、可随时查阅、可自学复刻、可二次开发的《deer-flow 全仓库源码解析知识库》完整MD文集；核心亮点是「每一处解析都附带设计思路+为什么这么做」，让学习者不仅懂代码，更懂代码背后的设计逻辑和工程考量。
```

## 二、配套：全局执行流程Mermaid总图（直接放进你的文档目录，含设计逻辑解读）

```mermaid
flowchart LR
A[初始化：AI加载完整deer-flow仓库] --> B[生成00-全集总览&全仓库拓扑架构.md]
B --> C[按「设计逻辑优先级+目录层级」：根目录→一级文件夹→二级子模块]
C --> D[每个独立模块自动生成专属MD文档]
D --> E[每篇标配：源码+路径+解读+Mermaid图+学习要点+设计思路（重点）]
E --> F[全覆盖：代码/配置/脚本/前端/后端/测试/示例/部署，同步解读各部分设计考量]
F --> G[全程无篇数限制，扒完整个仓库为止，保证设计逻辑连贯]
G --> H[最终合成完整源码知识库，可长期查阅复刻，重点体现设计思路闭环]
```

### 流程设计解读

1.  优先生成「全集总览」：核心设计考量是「先建立全局设计认知」，让学习者先明白项目整体设计目标、架构逻辑，再逐个模块深入，避免“只见树木不见森林”；

2.  按「设计逻辑优先级」解析：核心设计考量是「贴合项目开发与设计流程」，先解析核心业务模块（引擎、调度），再解析辅助模块（配置、日志），符合“先核心后辅助”的设计与学习逻辑；

3.  每篇强化设计思路：核心设计考量是「满足学习需求」，学习者不仅需要知道“代码是什么”，更需要知道“为什么这么写”，才能真正理解项目设计精髓，实现复用和二次开发。

## 三、补充2个补强指令（随时穿插用，强化设计思路解读）

### 1. 单独深挖某个文件夹/文件指令

```plain text
现在暂停顺序生成，单独深度精读：【填写文件夹/文件路径】，单独输出一篇超详细MD，补全调用链、底层细节、隐藏逻辑；重点强化「设计思路」解读，逐段说明「该文件/文件夹的设计初衷」「核心代码的设计考量」「与其他模块的设计关联」「设计取舍细节」，确保每一处逻辑都讲清「为什么这么做」。
```

### 2. 全仓收尾汇总指令（所有文档跑完后用）

```plain text
所有模块解析完毕，请输出最终全集汇总文档：《deer-flow 全项目核心精华总册.md》，整合所有关键架构、核心代码、复用技巧、二次开发全集指南；重点新增「全项目设计思路总览」模块，解读：
1.  项目整体设计理念和核心目标；
2.  各模块的设计关联与逻辑闭环；
3.  项目设计的核心亮点与行业差异化；
4.  设计过程中的整体取舍与考量；
5.  二次开发时如何贴合原有设计逻辑，避免破坏架构完整性。
```

## 四、修改说明（核心优化点）

- 1.  角色定位新增：重点强化「设计思路」解读，要求每一处核心逻辑讲清「为什么这么做」；

- 2.  通用规则新增：代码解读、图表解读均需补充设计考量，设计思路模块占比不低于20%；

- 3.  拆分原则优化：结合「设计逻辑+目录层级」拆分，每一次拆分都说明设计层面的理由；

- 4.  单篇模板强化：新增「设计初衷」「图表设计解读」，扩充「底层设计思想」模块，每部分都紧扣「为什么这么做」；

- 5.  执行流程优化：按「设计逻辑优先级」解析，保证设计逻辑连贯；

- 6.  补强指令优化：新增设计思路相关解读要求，汇总文档新增「全项目设计思路总览」；

- 7.  新增流程设计解读：说明全局执行流程的设计考量，贴合学习和项目设计逻辑。 